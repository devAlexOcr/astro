CREATE TABLE Messier (
    IdMessier INTEGER(11),
    Type VARCHAR(50) NOT NULL,
    Name VARCHAR(50) NOT NULL,
    Constellation VARCHAR(50) NOT NULL,
    Visibilite VARCHAR(25) NOT NULL,
    PRIMARY KEY (idMessier)
)